interface IHero {
  name: string;
  url: string;
}
interface Proyecto {
  titulo: string;
  descripcion: string;
  zonas: string;
  enlace: string;
}
interface ContactData {
  name: string;
  email: string;
  message: string;
  subject: string;
}
